components:
  schemas:
    Schema:
      type: object
      properties:
        ref:
          type: string
        type:
          type: string
        properties:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              value:
                $ref: '#/components/schemas/Schema'
        enum:
          type: array
          items:
            type: object